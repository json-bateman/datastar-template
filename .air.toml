root = "."
testdata_dir = "testdata"
tmp_dir = "tmp"

[build]
args_bin = []
entrypoint = "tmp/app"
cmd = "go tool templ generate && go build -o tmp/app ./cmd/app"
include_ext = ["go", "templ", "html", "css"]
exclude_regex = ["_templ\\.go$"]
